# server.port=5000
# Database Configuration
spring.datasource.url=jdbc:mysql://${RDS_HOSTNAME:localhost}:${RDS_PORT:3306}/${RDS_DB_NAME:transport}
spring.datasource.username=${RDS_USERNAME:root}
spring.datasource.password=${RDS_PASSWORD:root}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Mail Configuration (Spring Boot Mail)
spring.mail.host=smtp.gmail.com
spring.mail.port=587

#Email
spring.mail.username=SenderEmail 
#Password
spring.mail.password=SenderPassword

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.debug=true

# MVC Configuration
spring.mvc.pathmatch.matching-strategy=ANT_PATH_MATCHER
spring.main.allow-circular-references=true

# Twilio Configuration
twilio.accountSid=AC1b4b9339eaa26532e31e460d3ea044c0
twilio.authToken=91efdc88769549a90f8c04dbb14b64f7
twilio.trialNumber=+18573715725

# Multipart limits
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# File-upload directory
file.upload-dir=uploads
